[build]
builder = "NIXPACKS"
buildCommand = "echo 'Cache cleared - rebuild' && cd server && npm install && cd ../client && npm install && npm run build"
sourceDir = "server"

[deploy]
startCommand = "node index.js"

[environment]
NODE_ENV = "production"
