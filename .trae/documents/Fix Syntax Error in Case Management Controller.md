I will fix the syntax error in `server/controllers/caseManagementController.js` that caused the server crash. This error occurred due to a malformed code update in the previous steps.

### **Plan**
1.  **Repair `updateCaseStatus` Method**: I will rewrite the `updateCaseStatus` method in `server/controllers/caseManagementController.js` to correctly implement the case update and notification logic, ensuring valid JavaScript syntax.
2.  **Clean up `exportReport` Method**: I will remove the duplicate error handling code at the end of the `exportReport` method.

This will resolve the `SyntaxError: Unexpected token ')'` and get the server running again.
