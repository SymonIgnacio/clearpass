I will reset your local repository to match the state of commit `ac3097c`.

**Warning:** This operation uses `git reset --hard`.

1. **Discard Changes:** Any changes in commit `600c3ac` ("For Deployment Testing V1") will be discarded.
2. **Overwrite Files:** Your working directory will be updated to match `ac3097c` exactly. Any uncommitted local changes will be lost.

**Step 1: Execute Git Reset**

* Run `git reset --hard ac3097c` to move HEAD and update files.

